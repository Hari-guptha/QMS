export enum UserRole {
    CUSTOMER = 'customer',
    AGENT = 'agent',
    ADMIN = 'admin',
}

export enum TicketStatus {
    PENDING = 'pending',
    CALLED = 'called',
    SERVING = 'serving',
    COMPLETED = 'completed',
    HOLD = 'hold',
    CANCELLED = 'cancelled',
}
